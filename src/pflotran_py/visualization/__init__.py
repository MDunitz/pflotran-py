"""PFLOTRAN reactive-transport post-processing: extraction, gradient/flux
computation, and 2D/3D visualization.

Moved from the top-level ``visualization/`` directory into the
``pflotran_py`` package (src layout). Module names are unchanged in this
move; structural splits (compute vs. presentation) follow separately.
"""
